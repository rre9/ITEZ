# شرح المشروع الكامل - IT Help Desk System

## 🎯 نظرة عامة

**نظام IT Help Desk** هو نظام شامل لإدارة تذاكر الدعم الفني مبني على أحدث التقنيات.

---

## 🛠️ التقنيات المستخدمة

### **Backend (الخلفية)**

| التقنية | الإصدار | الوصف |
|---------|---------|-------|
| **ASP.NET Core** | 8.0 | إطار عمل تطبيقات الويب |
| **C#** | 12.0 | لغة البرمجة |
| **Entity Framework Core** | 8.0.21 | ORM للتعامل مع قاعدة البيانات |
| **SQL Server** | LocalDB/Full | خادم قاعدة البيانات |

### **Frontend (الواجهة)**

| التقنية | الوصف |
|---------|-------|
| **Bootstrap 5** | إطار عمل CSS للتصميم المتجاوب |
| **jQuery** | مكتبة JavaScript |
| **Font Awesome** | مكتبة الأيقونات |
| **Razor Views (MVC)** | عرض من جانب الخادم (MVC Pattern) |

**ملاحظة مهمة**: المشروع يستخدم **Razor Views** مع **MVC Controllers** وليس Razor Pages Framework.

---

## 🗄️ قاعدة البيانات

### **الجداول الرئيسية:**

1. **AspNetUsers** (المستخدمون)
   - معلومات المستخدمين
   - كلمات المرور المشفرة (PBKDF2)
   - الأدوار والصلاحيات

2. **Tickets** (التذاكر)
   - معلومات التذاكر
   - الحالة، الأولوية، القسم
   - منشئ التذكرة والمعين عليها

3. **TicketAttachments** (المرفقات)
   - الملفات المرفقة بالتذاكر
   - الصور، PDF، إلخ

4. **TicketLogs** (سجلات النشاط)
   - سجل كامل لجميع الإجراءات
   - من قام بالإجراء ومتى

### **العلاقات:**

```
مستخدم (1) ──→ (كثير) تذاكر (منشئ)
مستخدم (1) ──→ (كثير) تذاكر (معين)
تذكرة (1) ──→ (كثير) مرفقات
تذكرة (1) ──→ (كثير) سجلات نشاط
```

---

## 🔒 الأمان والتشفير

### **1. تشفير كلمات المرور**

- **الخوارزمية**: **PBKDF2**
- **التكرار**: 100,000 مرة
- **Salt**: فريد لكل كلمة مرور
- **النتيجة**: لا يمكن استرجاع كلمة المرور

**مثال:**
```
كلمة المرور: "Admin#12345!"
↓
PBKDF2 Hash: "AQAAAAIAAYagAAAAE..."
↓
مخزنة في: AspNetUsers.PasswordHash
```

### **2. Data Protection API**

- **الخوارزمية**: **AES-256-CBC**
- **الغرض**: تشفير Cookies و Tokens
- **دوران المفاتيح**: كل 90 يوم تلقائياً

**ما يتم تشفيره:**
- ✅ Cookies المصادقة
- ✅ Anti-forgery tokens
- ✅ Password reset tokens
- ✅ Email confirmation tokens

### **3. HTTPS**

- **البروتوكول**: TLS 1.2+
- **الغرض**: تشفير البيانات أثناء النقل
- **الإعداد**: تلقائي في ASP.NET Core

### **4. حماية CSRF**

- **الطريقة**: Anti-forgery tokens
- **الغرض**: منع هجمات Cross-Site Request Forgery
- **التنفيذ**: تلقائي في جميع النماذج

### **5. Rate Limiting**

- **Global**: 100 طلب في الدقيقة
- **Login**: 5 محاولات كل 15 دقيقة
- **الغرض**: منع هجمات Brute Force

### **6. User Secrets**

- **الموقع**: خارج الكود المصدري
- **الغرض**: حفظ كلمات المرور الحساسة
- **الأمان**: لا يُرفع إلى GitHub

---

## 🔐 المصادقة والتفويض

### **المصادقة (Authentication)**

- **الإطار**: ASP.NET Core Identity
- **الطريقة**: Cookie-based authentication
- **التشفير**: PBKDF2 لكلمات المرور

**عملية تسجيل الدخول:**
1. المستخدم يدخل البريد وكلمة المرور
2. النظام يشفر كلمة المرور ويقارنها
3. إذا تطابقت، ينشئ Cookie مصادقة
4. Cookie مشفر باستخدام Data Protection API

### **التفويض (Authorization)**

#### **1. Role-Based (RBAC)**

**الأدوار:**
- **Admin**: وصول كامل للنظام
- **Support**: إدارة التذاكر
- **Employee**: وصول محدود (تذاكره فقط)

#### **2. Resource-Based**

**القاعدة:**
- المستخدمون يرون فقط التذاكر التي أنشأوها أو عُينوا عليها
- Admin/Support يرون جميع التذاكر

---

## 📁 هيكل المشروع

```
ITHelpDesk/
├── Controllers/          # المتحكمات (المنطق)
│   ├── AdminController.cs
│   ├── TicketsController.cs
│   └── HomeController.cs
├── Models/              # النماذج (قاعدة البيانات)
│   ├── ApplicationUser.cs
│   ├── Ticket.cs
│   └── TicketLog.cs
├── Services/            # الخدمات (المنطق التجاري)
│   ├── TicketAttachmentService.cs
│   └── SmtpEmailSender.cs
├── Views/               # العروض (الواجهة)
│   ├── Tickets/
│   └── Admin/
├── Data/                # قاعدة البيانات
│   └── ApplicationDbContext.cs
└── wwwroot/             # الملفات الثابتة
    └── uploads/         # الملفات المرفوعة (⚠️ للتطوير فقط)
```

---

## 🔌 نقاط النهاية الرئيسية

### **التذاكر:**
- `GET /Tickets` - جميع التذاكر (Admin/Support)
- `GET /Tickets/MyTickets` - تذاكري (Employee)
- `GET /Tickets/Create` - إنشاء تذكرة
- `POST /Tickets/Create` - حفظ تذكرة
- `GET /Tickets/Details/{id}` - تفاصيل تذكرة
- `POST /Tickets/ChangeStatus/{id}` - تحديث الحالة

### **الإدارة:**
- `GET /Admin` - لوحة الإدارة
- `POST /Admin/ChangePassword` - تغيير كلمة المرور
- `POST /Admin/DeleteUser/{id}` - حذف مستخدم
- `POST /Admin/DeleteAllTickets` - حذف جميع التذاكر

---

## ⚙️ الإعدادات

### **appsettings.json**
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=...;Database=ITHelpDesk;..."
  },
  "Departments": {
    "Items": ["IT Operations", "Networking", "Security"]
  }
}
```

### **User Secrets (للتطوير)**
```json
{
  "Seed:AdminDefaultPassword": "Admin#12345!",
  "EmailSettings:Password": "your-password"
}
```

---

## 📊 ملخص الأمان

| الميزة | الحالة | الطريقة |
|--------|--------|---------|
| **تشفير كلمات المرور** | ✅ | PBKDF2 (100,000 iteration) |
| **تشفير البيانات** | ✅ | AES-256-CBC |
| **HTTPS** | ✅ | TLS 1.2+ |
| **حماية CSRF** | ✅ | Anti-forgery tokens |
| **منع SQL Injection** | ✅ | Parameterized queries |
| **Rate Limiting** | ✅ | 5 محاولات / 15 دقيقة |
| **التفويض** | ✅ | RBAC + Resource-based |
| **إدارة الأسرار** | ✅ | User Secrets |

---

## 🔑 أنواع التشفير المستخدمة

### **1. PBKDF2 (كلمات المرور)**
- **النوع**: One-way hash
- **الخوارزمية**: PBKDF2 with HMAC-SHA256
- **التكرار**: 100,000
- **الغرض**: تخزين كلمات المرور بأمان

### **2. AES-256-CBC (البيانات)**
- **النوع**: Symmetric encryption
- **الخوارزمية**: AES-256-CBC
- **الغرض**: تشفير Cookies و Tokens

### **3. TLS (الاتصالات)**
- **النوع**: Transport layer encryption
- **البروتوكول**: TLS 1.2+
- **الغرض**: تشفير البيانات أثناء النقل

---

## 📝 الخلاصة

### **التقنيات:**
- ✅ ASP.NET Core 8.0 MVC
- ✅ Entity Framework Core 8.0
- ✅ SQL Server
- ✅ ASP.NET Core Identity
- ✅ Bootstrap 5

### **الأمان:**
- ✅ PBKDF2 لتشفير كلمات المرور
- ✅ AES-256 لتشفير البيانات
- ✅ HTTPS/TLS للاتصالات
- ✅ حماية CSRF
- ✅ Rate Limiting
- ✅ User Secrets

### **البنية:**
- ✅ نمط MVC
- ✅ بنية طبقات
- ✅ Dependency Injection
- ✅ Service-oriented design

---

**آخر تحديث**: 2024
**الإصدار**: 1.0

